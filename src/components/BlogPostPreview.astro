---
export interface Props {
  post: any;
}

const { post } = Astro.props;
---
<!-- <article class="post-preview">
  <header>
    <h3 class="publish-date">{post.publishDate}</h3>
    <a href={post.url}><h1 class="title">{post.title}</h1></a>
  </header>
  <main>
    <p>{post.description}</p>
    <a href={post.url}>Read more</a>
  </main>
</article> -->
<ks-card>
  <ks-card-body card-title={post.title} href={post.url} card-subtitle={post.publishDate}>
    <ks-card-footer>
        <ks-button href={post.url}>Read More</ks-button>
    </ks-card-footer>
  </ks-card-body>
  </ks-card>
<style>
</style>
